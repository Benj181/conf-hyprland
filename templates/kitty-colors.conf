# GENERATED from palettes/{{palette}}.env by scripts/theme.sh -- do not edit.
# Edit the palette and re-run the script.

foreground            {{text}}
background            {{bg}}
selection_foreground  {{bg}}
selection_background  {{text}}

cursor                {{accent}}
cursor_text_color     {{bg}}

url_color             {{dim}}

# The window border colours kitty draws between splits, matched to Hyprland's
# so a kitty split and a Hyprland split look like the same kind of seam.
active_border_color   {{accent}}
inactive_border_color {{border}}
bell_border_color     {{urgent}}

active_tab_foreground   {{text}}
active_tab_background   {{surface}}
active_tab_font_style   normal
inactive_tab_foreground {{muted}}
inactive_tab_background {{bg_alt}}
inactive_tab_font_style normal
tab_bar_background      {{bg}}

mark1_foreground {{bg}}
mark1_background {{dim}}
mark2_foreground {{bg}}
mark2_background {{muted}}
mark3_foreground {{bg}}
mark3_background {{border}}

# --- ANSI 0-15 --------------------------------------------------------------
# colour0/7/8/15 come off the greyscale ramp; the six hues are the palette's
# desaturated set. Making these achromatic too would be more "correct" for a
# monochrome rice and would break `git diff`, `ls`, `grep --color` and every
# compiler's error output at once -- red and green have to stay
# distinguishable, so they are kept at the lowest saturation that survives it.
color0  {{surface}}
color8  {{muted}}

color1  {{red}}
color9  {{red_br}}

color2  {{green}}
color10 {{green_br}}

color3  {{yellow}}
color11 {{yellow_br}}

color4  {{blue}}
color12 {{blue_br}}

color5  {{magenta}}
color13 {{magenta_br}}

color6  {{cyan}}
color14 {{cyan_br}}

color7  {{text}}
color15 {{accent}}
